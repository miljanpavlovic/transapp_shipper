<resources>

    <!-- Base application theme. -->
    <style name="Theme.Shipper" parent="Theme.AppCompat.NoActionBar">
        <!-- customize the color palette -->
        <item name="colorPrimary">@color/app_background_gray</item>
        <item name="colorPrimaryDark">@color/app_dark_gray</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>

        <!-- Active thumb color & Active track color(30% transparency) -->
        <item name="colorControlActivated">@color/colorAccent</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/app_background_gray</item>

    </style>

    <style name="ActivitySplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
